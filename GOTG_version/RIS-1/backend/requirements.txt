Flask==2.3.3
Flask-CORS==4.0.0
python-dotenv==1.0.0
PyJWT==2.8.1
Werkzeug==2.3.7
requests==2.31.0

# ML & Biometric Detection (Optional - lightweight alternatives)
face-recognition==1.3.5  # Lightweight facial recognition (~50MB)
opencv-python==4.8.0.76  # Computer vision (optional, for fingerprint processing)
Pillow==10.0.0  # Image processing
numpy==1.24.0  # Numerical computing

# QR Code Generation
qrcode==7.4.2  # QR code generation
python-qrcode==1.0  # Alternative QR library

# Data Processing & Compression
python-dateutil==2.8.2
pytz==2023.3

# Disaster Recovery & Extreme Bandwidth (NEW)
brotli==1.0.9  # Brotli compression (better than gzip, optional fallback to gzip)
pytesseract==0.3.10  # OCR for document scanning (optional)
scikit-learn==1.3.0  # ML utilities for text processing
lxml==4.9.3  # XML parsing for backup recovery
